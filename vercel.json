{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo \"Dependencies are installed by buildCommand inside web/\"",
  "buildCommand": "cd web && npm ci --no-audit --no-fund && npm run build",
  "outputDirectory": "web/out"
}
